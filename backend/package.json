{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "description": "",
  "keywords": [],
  "author": "Josue Leandro Navarro Ribeiro",
  "license": "ISC",
  "scripts": {
    "dev": "node generateSecretJWT.js && npm run migrate && ts-node-dev --respawn --transpile-only src/app.ts",
    "build": "npm run clean && tsc",
    "start": "node generateSecretJWT.js && npm run migrate && node dist/app.js",
    "migrate": "ts-node src/runMigrations.ts",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "crypto": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.9.0",
    "@types/pg": "^8.11.10",
    "ts-node": "^10.2.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
